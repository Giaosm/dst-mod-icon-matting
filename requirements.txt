# AI 抠图工具依赖 (Python 3.13)
# Web 界面
gradio>=5.0
# ONNX 推理 (RMBG-2.0 / BEN2)
onnxruntime>=1.19
# PyTorch 推理 (BiRefNet / FeyNoBg / InSPyReNet)，macOS 自动带 MPS 支持
torch>=2.5
torchvision>=0.20
# transformers 格式 (FeyNoBg)
transformers>=4.44
safetensors>=0.4
huggingface_hub>=0.24
# BiRefNet 架构代码依赖
timm>=1.0
einops>=0.8
kornia>=0.7
# 图像与通用
pillow>=10.0
numpy>=1.26
requests>=2.31
filelock>=3.13

# ---- 可选模型依赖 (安装失败不影响其它引擎) ----
# FeyNoBg: pip install "nobg>=0.2.5"
# InSPyReNet: pip install transparent-background

